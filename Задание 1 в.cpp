#include <iostream>
using namespace std;

int sumRun()
{
    int distance;
    int sumDays = distance;
    int days;
    for (int i = days; i > 1; --i)
    {
        distance += distance * 0.1;
        sumDays += distance;
    }
    return 0;
}